// Placeholder — the record tab button navigates to /record via the tab layout listener.
// This file exists only to satisfy Expo Router's file-based routing requirement.
import { View } from "react-native";

export default function RecordPlaceholder() {
  return <View />;
}
